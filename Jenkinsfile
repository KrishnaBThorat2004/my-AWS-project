pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/KrishnaBThorat2004/my-AWS-project.git'
            }
        }

        stage('Build') {
            steps {
                echo 'Build Success 🚀'
            }
        }
    }
}
